package brutelol.characters.lib;

/**
 * An Enum containing all masteries a champion can have equipped.
 * Masteries with 1-5 levels are treated as being boolean (0-5) because I basically
 * NEVER see anybody splitting them up.
 * @author Talonos
 */
public enum Mastery 
{
    BUTCHER,
    FEAST,
    EXECUTIONER_1,EXECUTIONER_2,EXECUTIONER_3,
    DANGEROUS_GAME,
    HAVOC,
    FRENZY,
}
